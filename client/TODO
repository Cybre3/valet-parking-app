LOGIN
    IF EMPLOYEE
        ENTER EMPLOYEE ID 
            (8 DIGITS LAST INITIAL, FIRST INITIAL, BIRTHDATE)
        ENTER PIN 
            (6 DIGITS, RANDOM 6 NUMBERS)
    IF GUEST
        ENTER EMAIL
    ADD BUTTON TO SEND EMAIL FOR VERIFICATION AND REGISTRATION
    OTP

USERS
    GIVE USER ABILITY TO DELETE ACCOUNT FROM users COLLECTION
    CREATE all-visitors COLLECTION FOR EVERYONE THT HAS CREATED ACCOUNT

INTEGRATE EXTERNAL CARS API

REMOVE/RETURN CAR
    IF carInTransit IS FALSE
        REQUIRE EMPLOYEE PASSWORD TO REMOVE CAR
    ELSE
        REMOVE CAR

    WHEN CAR REMOVED/RETURNED ADD TO returned-cars COLLECTION IN DATABASE AND? STORE?
    ADD TIMED DELETE RULE - DELETION AFTER 30 DAYS OF CAR RETURN

OPTIMIZE FOR MOBILE